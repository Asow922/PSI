{% extends 'base.html.twig' %}

{% block body %}
    <h1>Kartaprzedmiotu</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kartaPrzedmiotu.id }}</td>
            </tr>
            <tr>
                <th>Wersja</th>
                <td>{{ kartaPrzedmiotu.wersja }}</td>
            </tr>
            <tr>
                <th>Studium</th>
                <td>{{ kartaPrzedmiotu.studium }}</td>
            </tr>
            <tr>
                <th>Opiekunprzedmiotu</th>
                <td>{{ kartaPrzedmiotu.opiekunPrzedmiotu }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('kartaprzedmiotu_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('kartaprzedmiotu_edit', { 'id': kartaPrzedmiotu.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
