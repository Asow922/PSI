{% extends 'base.html.twig' %}

{% block body %}
    <h1>Grupa kursów tworzenie</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Utwórz" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('przedmiot_show', {'id': grupaKursow.przedmiot.id}) }}">Wróć do przedmiotu</a>
        </li>
    </ul>
{% endblock %}
