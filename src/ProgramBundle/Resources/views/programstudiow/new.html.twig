{% extends 'base.html.twig' %}

{% block body %}
    <h1>Program studiów tworzenie</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('kierunekstudiow_show', {'id': programStudiow.programKsztalcenia.id}) }}">Wróć do kierunku studiów</a>
        </li>
    </ul>
{% endblock %}
