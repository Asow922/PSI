{% extends 'base.html.twig' %}

{% block body %}
    <h1>Plan studiów edycja</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input type="submit" value="Edycja"/>
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('kierunekstudiow_show', {'id': planStudiow.programStudiow.programKsztalcenia.kierunekStudiow.id }) }}">Wróć
                do kierunku studiów</a>
        </li>
        <li>
            <a href="{{ path('planstudiow_show', {'id': planStudiow.id }) }}">Wróć
                do planu studiów</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Usuń">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
