{% extends 'base.html.twig' %}

{% block body %}
    <h1>Efekt ministerialny</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ efektMinisterialny.id }}</td>
            </tr>
            <tr>
                <th>Identyfikator</th>
                <td>{{ efektMinisterialny.identyfikator }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ efektMinisterialny.opis }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('efektministerialny_index') }}">Powrót</a>
        </li>
        <li>
            <a href="{{ path('efektministerialny_edit', { 'id': efektMinisterialny.id }) }}">Edycja</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
