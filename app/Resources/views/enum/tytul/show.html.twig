{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tytul</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tytul.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tytul.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('enum_tytul_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('enum_tytul_edit', { 'id': tytul.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
