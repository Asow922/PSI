{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sposobzaliczenium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sposobZaliczenium.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sposobZaliczenium.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('enum_sposobzaliczenia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('enum_sposobzaliczenia_edit', { 'id': sposobZaliczenium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
