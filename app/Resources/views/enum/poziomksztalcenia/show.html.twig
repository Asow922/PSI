{% extends 'base.html.twig' %}

{% block body %}
    <h1>Poziomksztalcenium</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poziomKsztalcenium.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ poziomKsztalcenium.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('enum_poziomksztalcenia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('enum_poziomksztalcenia_edit', { 'id': poziomKsztalcenium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
