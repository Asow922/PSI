{% extends 'base.html.twig' %}

{% block body %}
    <h1>Wydzial</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wydzial.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ wydzial.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('enum_wydzial_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('enum_wydzial_edit', { 'id': wydzial.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
