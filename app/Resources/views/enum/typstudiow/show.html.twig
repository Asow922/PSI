{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typstudiow</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typStudiow.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ typStudiow.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('enum_typstudiow_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('enum_typstudiow_edit', { 'id': typStudiow.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
