{% extends 'base.html.twig' %}

{% block body %}
    <h1>Zakre</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zakre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ zakre.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('enum_zakres_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('enum_zakres_edit', { 'id': zakre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
