{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rodzaj</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rodzaj.id }}</td>
            </tr>
            <tr>
                <th>Shortcode</th>
                <td>{{ rodzaj.shortCode }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rodzaj.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('enum_rodzaj_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('enum_rodzaj_edit', { 'id': rodzaj.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
